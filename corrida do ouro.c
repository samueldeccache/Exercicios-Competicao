#include <stdio.h>

int main(){
int c; //capital inicial
int N; //tempo
int d; //diferença
float t; //taxa entre d/valor_1
float m; //montante final
int valor;
int i;

scanf("%d %d",&c,&N);
if(c>=0 && c<=3000 && N>=0 && N<=30){
    for (i=1;i<=N-1;i++){
        scanf("%d\n",&valor);
        int vet[i];
    }
    printf("%d",vet[2]);
}
}
